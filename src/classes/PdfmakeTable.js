class PdfMakeTable {
  container() {
    /** @type {import('pdfmake/interfaces').Table} */
    const table3 = {
      headerRows: 2,
      dontBreakRows: true,
      widths: ['auto', 60, '*'],
      body: [],
    }

    /** @type {import('pdfmake/interfaces').TDocumentDefinitions} */
    const tt = {}
  }

  
}
